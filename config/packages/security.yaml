security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|assets|build|api/docs)/
            security: false

        api:
            pattern: ^/api/v1
            stateless: true
            jwt: ~

        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: login
                check_path: login
                default_target_path: post_login
                username_parameter: email
                password_parameter: password
                enable_csrf: true
            logout:
                path: logout
                target: home
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800

    access_control:
        - { path: ^/webhooks/telegram,               roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/auth,                    roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/organizations,            roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/busy-slots,               roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/client,                   roles: ROLE_CLIENT }
        - { path: ^/api/v1/business,                 roles: ROLE_BUSINESS }
        - { path: ^/api/v1/employee,                 roles: ROLE_EMPLOYEE }
        - { path: ^/api,                             roles: PUBLIC_ACCESS }
        - { path: ^/orgs,     roles: PUBLIC_ACCESS }
        - { path: ^/business, roles: ROLE_BUSINESS }
        - { path: ^/employee, roles: ROLE_EMPLOYEE }
        - { path: ^/client,   roles: ROLE_CLIENT }

when@test:
    security:
        password_hashers:
            App\Entity\User:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
