nelmio_api_doc:
    documentation:
        info:
            title: Grafira Appointy API
            description: RESTful API for the Grafira appointment booking platform
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api/v1
